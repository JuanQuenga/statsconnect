# Platform backend

`packages/backend/convex` is the only deployable Convex tree.

Read `convex/_generated/ai/guidelines.md` before changing Convex code. Its
Convex rules override general repository guidance.

Run every Convex command from this package. Do not create `apps/*/convex`
directories.

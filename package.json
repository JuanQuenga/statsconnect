{
  "name": "statsconnect-workspace",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.12.3",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @statsconnect/backend --filter statsconnect dev",
    "dev:hub": "pnpm --filter statsconnect dev",
    "dev:brawlstats": "pnpm --filter brawlstats.io dev",
    "dev:clashcrown": "pnpm --filter clash-crown dev",
    "build:unified": "STATSCONNECT_UNIFIED_BUILD=1 pnpm --filter statsconnect build && STATSCONNECT_UNIFIED_BUILD=1 pnpm --filter brawlstats.io build && STATSCONNECT_UNIFIED_BUILD=1 pnpm --filter clash-crown build",
    "deploy:backend": "pnpm --filter @statsconnect/backend deploy",
    "deploy:unified": "pnpm --filter @statsconnect/backend deploy:with-frontend",
    "typecheck": "pnpm -r --if-present typecheck"
  }
}
